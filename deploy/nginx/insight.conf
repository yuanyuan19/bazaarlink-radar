server {
    listen 80;
    server_name mirror.example.com;
    location / {
        proxy_pass http://mirror:8787;
        proxy_set_header Host $host;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}
